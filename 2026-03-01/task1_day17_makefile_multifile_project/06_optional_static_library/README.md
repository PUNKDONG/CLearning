# 练习6（可选）：静态库拆分

## 目标
理解“模块先打包成库，再被主程序链接”。

## 你要完成
1. 把工具函数打包为静态库（`libxxx.a`）。
2. `main` 只链接库，不直接链接工具模块 `.o`。
3. Makefile 添加库目标及依赖。
4. `clean` 同时清理库与可执行文件。

## 建议文件结构（你自己创建）
- `main.c`
- `math_utils.c`
- `math_utils.h`
- `Makefile`

## 验收
1. 能生成静态库和最终程序。
2. 改库源码后，`make` 会更新库并重新链接。
