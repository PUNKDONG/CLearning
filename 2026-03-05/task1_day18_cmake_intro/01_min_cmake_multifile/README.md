# 01_min_cmake_multifile

## 你需要完成

1. 补齐 `CMakeLists.txt` 里的 TODO
2. 实现 `src/utils.c` 的 TODO
3. 完成 `src/main.c` 的调用逻辑

## 构建命令

```bash
cmake -S . -B build
cmake --build build
./build/app
```

## 自检

1. 代码能成功构建
2. 运行结果不是 TODO 文案
3. 开启 `-Wall -Wextra` 后无关键警告

