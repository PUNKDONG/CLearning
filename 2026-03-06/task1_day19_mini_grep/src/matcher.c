#include "matcher.h"

/*
 * TODO:
 * 1) ignore_case == 0: 使用区分大小写匹配
 * 2) ignore_case != 0: 实现忽略大小写匹配
 * 3) line 或 keyword 为 NULL 时返回 0
 */
int line_matches(const char *line, const char *keyword, int ignore_case) {
    (void)line;
    (void)keyword;
    (void)ignore_case;
    return 0;
}
